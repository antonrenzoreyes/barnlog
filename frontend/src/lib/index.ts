// Place files you want to import through the `$lib` alias in this folder.
// This file is currently a placeholder and has a temporary Biome override in
// `frontend/biome.jsonc` (`overrides` -> `includes: ["src/lib/index.ts"]`).
// Remove that override once this barrel exports real modules.
